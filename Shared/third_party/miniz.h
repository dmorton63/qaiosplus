#pragma once

// Minimal wrapper header to satisfy miniz_tinfl.c's include of "miniz.h".
// We only use the tinfl (inflate) portions of miniz in this codebase.

#include "miniz_common.h"
#include "miniz_tinfl.h"
